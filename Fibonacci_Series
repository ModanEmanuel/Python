def fibonacci(n):
    l = [0, 1]
    for i in range(2, n):
        l.append(l[-1]+l[-2])
    return l
if __name__ == "__main__":
    n = int(input("How many Fibonacci numbers ?"))
    fibo = fibonacci(n)
    print ("Fibonacci series :", *fibo)
